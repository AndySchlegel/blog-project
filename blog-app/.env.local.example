# MongoDB Connection
# Lokal: mongodb://localhost:27017/blog-db
# Atlas: mongodb+srv://user:pass@cluster.mongodb.net/blog-db
MONGODB_URI=mongodb://localhost:27017/mein-blog

# JWT Secret für Token-Signierung
# Nutze einen starken, zufälligen String für Production!
# Generierung: openssl rand -base64 32
JWT_SECRET=change-me-to-a-secure-random-string

# Cookie Security
# false für lokale Entwicklung (HTTP)
# true für Production (HTTPS)
AUTH_COOKIE_SECURE=false

# Optional: Node Environment
# NODE_ENV=development
